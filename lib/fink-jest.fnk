
{parse} = require:: `@fink/larix`
{generate} = require:: `@fink/loxia`


transform = fn source, filename:
  fink_ast = parse(source)
  {code} = generate(fink_ast, filename, source)
  code


process = fn src, filename:
  transform(src, filename)

